name: TileMapper
version: '${project.version}'
main: com.mndk.tilemapper.TileMapper
api-version: '1.21'
prefix: TSM
load: STARTUP

dependencies:
  server:
    Terraplusminus:
      load: BEFORE
      required: true
      join-classpath: true

permissions:
  tilemapper.admin:
    description: 允許使用 /tsm 指令
    default: op
